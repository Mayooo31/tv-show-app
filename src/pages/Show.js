import React, { Fragment } from "react";
import ShowItem from "../components/ShowItem";

const Show = () => {
  return (
    <Fragment>
      <ShowItem />
    </Fragment>
  );
};

export default Show;
